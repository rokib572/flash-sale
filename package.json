{
  "name": "flash-sale",
  "private": true,
  "version": "0.1.0",
  "description": "Monorepo for a high-throughput flash sale system",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "seed:users": "tsx scripts/seed-users.ts",
    "lint:watch": "eslint . --ext .ts,.tsx,.js --watch",
    "format": "prettier --write .",
    "stress": "tsx scripts/stress.ts"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "eslint": "^8.57.0",
    "prettier": "^3.3.3",
    "tsx": "^4.19.0",
    "turbo": "^2.5.6",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "build": "^0.1.4"
  }
}
